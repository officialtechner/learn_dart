void main() {
  /* var lst = [1, 2, 3];
  lst.add(6); //  add single  value to list.
  lst.addAll([7, 8, 9]); // add multiple values to list.
  lst.insert(3, 4); // insert value to particular location.
  lst.insertAll(1, [7, 8, 9]); // insert multiple values to particular location.
  lst[2] = 7; // update list
  lst.replaceRange(0, 2, [11, 12, 13]); //replace values from particular location.
  lst.remove(1); // remove the value.
  lst.removeAt(3); //remove value from specific location.
  lst.removeLast(); //remove the last element of set.
  print(lst.length); //length of list.
  lst.removeRange(0, 1); //remove the value from given range.
  print(lst);*/
  var list = ['hello'];
  list.add('1');
  list.add("Negi");
  print(list);
}
